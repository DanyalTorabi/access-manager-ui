/** Single source of truth for the API base URL used in tests.
 *  Must match VITE_API_BASE_URL in vitest.config.ts define. */
export const TEST_API_BASE = 'http://127.0.0.1:8080'

/** Shared fixture domain ID used across API and hook tests. */
export const TEST_DOMAIN_ID = 'dom-1'
